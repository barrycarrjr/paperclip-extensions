/**
 * UI bundle entrypoint for todo-list.
 *
 * Exports the React components named in the manifest's `ui.slots` so the
 * Paperclip host can mount them.
 */

export { TodoSidebarItem } from "./TodoSidebarItem.js";
export { TodoPage } from "./TodoPage.js";
