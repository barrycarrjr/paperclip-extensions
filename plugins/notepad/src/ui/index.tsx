/**
 * UI bundle entrypoint for notepad.
 *
 * Exports the React components named in the manifest's `ui.slots` so the
 * Paperclip host can mount them.
 */

export { NotepadSidebarItem } from "./NotepadSidebarItem.js";
export { NotepadPage } from "./NotepadPage.js";
